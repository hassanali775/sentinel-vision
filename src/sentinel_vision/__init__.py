"""Sentinel Vision: visual world-state and event reasoning engine for
industrial workspace monitoring and asset intelligence.

This package is intentionally empty beyond a version marker at this stage
(PR-001). No subpackages exist yet — capture, perception, tracking, and
every later layer are introduced organically in their own PRs, each
justified by the code it actually contains. See docs/adr/0001-deterministic
-state-is-source-of-truth.md for the foundational architectural decision
that all later PRs build on.
"""

__version__ = "0.0.1"

__all__ = ["__version__"]
